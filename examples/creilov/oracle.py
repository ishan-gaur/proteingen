# Load the data table with all replicates

# Look at the replicate correlations

# Standardize to a single replicate by subtracting the best-fit linear regressions to the reference

# Decide if any replicates should be thrown out

# Fit mu and sigma for each variant

# Train a few individual predictors and look at the results

# Train an ensemble of whatever looked good
# ensemble per replicate if they look normal after regressing the mean
# ensemble of training runs
