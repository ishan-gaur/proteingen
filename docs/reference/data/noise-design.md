# Noise Design

ProtStar separates **what corruption is applied** (`noise_fn`) from **how much corruption is applied** (`time_sampler`).

See the full explanation in [Data reference → Noise design](../data.md#noise-design).
