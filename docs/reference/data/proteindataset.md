# ProteinDataset

`ProteinDataset` is the core container for raw sequence data, observations, and optional labels.

For full details (including `collator()` behavior), see the main section in the [Data reference](../data.md#proteindataset).
